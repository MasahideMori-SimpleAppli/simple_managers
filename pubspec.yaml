name: simple_managers
description: A package containing multiple manager classes that support serialization.
version: 0.0.1
repository: https://github.com/MasahideMori-SimpleAppli/simple_managers

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
